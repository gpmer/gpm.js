language: node_js
sudo: false

os:
  - linux

node_js:
  - 7.6.0
  - 8.9.0

cache:
  yarn: true
  directories:
    - node_modules

before_script:
  - yarn run build

script:
  - yarn run test

after_script:
  - yarn run coverage
